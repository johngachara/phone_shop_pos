node_modules
build
dist
dev-dist
.env
.env.local
*.local

# Local-only session fixtures used to screenshot the authenticated UI without
# a passkey-capable authenticator. Never shipped: they hold real tokens.
public/__dev

.DS_Store
.idea
*.log
